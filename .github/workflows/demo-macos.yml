name: Demo macOS build

on:
  push:
    branches: [seeding-data-updated]

permissions:
  contents: read

concurrency:
  group: demo-macos-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:
  demo:
    name: Build, sign and draft the demo DMG
    runs-on: macos-15
    timeout-minutes: 120
    permissions:
      contents: write
    env:
      SIGN_MACOS: ${{ secrets.MACOS_CERT_P12 != '' }}
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-go@v7.0.0
        with:
          go-version-file: app/go.mod
          cache-dependency-path: app/go.sum
      - uses: actions/setup-node@v7.0.0
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: app/frontend/package-lock.json
      - name: Read the demo version
        id: version
        run: |
          version=$(sed -n 's/^CARE_DESKTOP_VERSION=//p' deployments/.env | tr -d '[:space:]')
          test -n "$version"
          echo "version=$version" >> "$GITHUB_OUTPUT"
      - name: Build the frontend
        working-directory: app/frontend
        run: |
          npm ci
          npm run build
      - name: Install matching Wails CLI
        working-directory: app
        run: go install github.com/wailsapp/wails/v2/cmd/wails@"$(go list -m -f '{{.Version}}' github.com/wailsapp/wails/v2)"
      - name: Build the clinic setup page
        run: node app/frontend/scripts/build-seed-page.mjs
      - name: Prepare installer metadata and deployment kit
        run: node app/frontend/scripts/stage-install.mjs
      - name: Build native application
        working-directory: app
        run: wails build -platform darwin/universal -clean -s -skipbindings -m -nosyncgomod
      - name: Import macOS signing certificate
        if: env.SIGN_MACOS == 'true'
        env:
          CERT_P12: ${{ secrets.MACOS_CERT_P12 }}
          CERT_PASSWORD: ${{ secrets.MACOS_CERT_PASSWORD }}
        run: |
          umask 077
          keychain="$RUNNER_TEMP/signing.keychain-db"
          keychain_pw=$(uuidgen)
          security create-keychain -p "$keychain_pw" "$keychain"
          security set-keychain-settings -lut 21600 "$keychain"
          security unlock-keychain -p "$keychain_pw" "$keychain"
          printf '%s' "$CERT_P12" | base64 --decode > "$RUNNER_TEMP/cert.p12"
          security import "$RUNNER_TEMP/cert.p12" -k "$keychain" -P "$CERT_PASSWORD" \
            -T /usr/bin/codesign -T /usr/bin/security
          rm -f "$RUNNER_TEMP/cert.p12"
          security set-key-partition-list -S apple-tool:,apple:,codesign: \
            -s -k "$keychain_pw" "$keychain" > /dev/null
          security list-keychains -d user -s "$keychain" \
            $(security list-keychains -d user | tr -d '"')
          security find-identity -v -p codesigning "$keychain"
          echo "SIGNING_KEYCHAIN=$keychain" >> "$GITHUB_ENV"
      - name: Sign, notarize and package the DMG
        env:
          VERSION: ${{ steps.version.outputs.version }}
          SIGN_IDENTITY: ${{ secrets.MACOS_SIGN_IDENTITY }}
          NOTARY_KEY: ${{ secrets.APPSTORE_PRIVATE_KEY }}
          NOTARY_KEY_ID: ${{ secrets.APPSTORE_KEY_ID }}
          NOTARY_ISSUER: ${{ secrets.APPSTORE_ISSUER_ID }}
        run: |
          mkdir -p release-assets staging
          apps=(app/build/bin/*.app)
          test "${#apps[@]}" -eq 1 && test -d "${apps[0]}"
          app="${apps[0]}"
          codesign --verify --strict "$app"
          if [ "$SIGN_MACOS" = "true" ]; then
            : "${SIGN_IDENTITY:?Missing MACOS_SIGN_IDENTITY}"
            : "${NOTARY_KEY:?Missing APPSTORE_PRIVATE_KEY}"
            : "${NOTARY_KEY_ID:?Missing APPSTORE_KEY_ID}"
            : "${NOTARY_ISSUER:?Missing APPSTORE_ISSUER_ID}"
            notary_key="$RUNNER_TEMP/notary.p8"
            (umask 077; printf '%s' "$NOTARY_KEY" > "$notary_key")
            notary=(--key "$notary_key" --key-id "$NOTARY_KEY_ID" --issuer "$NOTARY_ISSUER")
            field() { python3 -c 'import sys,json; print(json.load(sys.stdin)["'"$1"'"])'; }
            notarize() {
              local id status
              id=$(xcrun notarytool submit "$1" "${notary[@]}" \
                --no-wait --output-format json | field id)
              [ -n "$id" ] || { echo "::error::No notarization submission ID"; exit 1; }
              echo "submitted $1 -> $id"
              for attempt in 1 2 3 4 5 6; do
                xcrun notarytool wait "$id" "${notary[@]}" --timeout 2h && break
                echo "::warning::Notarization poll $attempt failed; retrying."
              done
              status=$(xcrun notarytool info "$id" "${notary[@]}" \
                --output-format json | field status)
              if [ "$status" != "Accepted" ]; then
                echo "::error::Notarization status: $status"
                xcrun notarytool log "$id" "${notary[@]}"
                exit 1
              fi
            }
            codesign --force --timestamp --options runtime \
              --keychain "$SIGNING_KEYCHAIN" --sign "$SIGN_IDENTITY" "$app"
            codesign --verify --strict "$app"
            ditto -c -k --keepParent "$app" notarize.zip
            notarize notarize.zip
            xcrun stapler staple "$app"
            xcrun stapler validate "$app"
          else
            echo "::warning::MACOS_CERT_P12 is absent; the build keeps its ad-hoc signature only."
          fi
          ditto "$app" "staging/CARE Desktop.app"
          ln -s /Applications staging/Applications
          dmg="release-assets/CARE-Desktop-$VERSION-demo-macos.dmg"
          hdiutil create -volname "CARE Desktop" -srcfolder staging -ov -format UDZO "$dmg"
          if [ "$SIGN_MACOS" = "true" ]; then
            codesign --force --timestamp \
              --keychain "$SIGNING_KEYCHAIN" --sign "$SIGN_IDENTITY" "$dmg"
            notarize "$dmg"
            xcrun stapler staple "$dmg"
            xcrun stapler validate "$dmg"
            spctl -a -vvv -t open --context context:primary-signature "$dmg"
          fi
          shasum -a 256 "$dmg" > release-assets/SHA256SUMS
      - name: Remove macOS signing credentials
        if: always() && env.SIGN_MACOS == 'true'
        run: |
          rm -f "$RUNNER_TEMP/cert.p12" "$RUNNER_TEMP/notary.p8"
          if [ -f "$RUNNER_TEMP/signing.keychain-db" ]; then
            security delete-keychain "$RUNNER_TEMP/signing.keychain-db"
          fi
      - uses: actions/upload-artifact@v7.0.1
        with:
          name: care-demo-macos
          path: release-assets/
          if-no-files-found: error
          retention-days: 14
      - name: Refresh the demo draft release
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          gh release delete demo --repo "$GITHUB_REPOSITORY" --yes --cleanup-tag || true
          gh release create demo --repo "$GITHUB_REPOSITORY" --target "$GITHUB_SHA" \
            --draft --prerelease --title "CARE Desktop demo (data loading)" \
            --notes "A demo build to test data loading.

          - Version: $VERSION
          - Source commit: $GITHUB_SHA
          - Build: $GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID
          - macOS only; signing: ${SIGN_MACOS/true/Developer ID + notarized}" \
            release-assets/*
